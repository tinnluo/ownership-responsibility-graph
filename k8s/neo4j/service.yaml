apiVersion: v1
kind: Service
metadata:
  name: neo4j-service
  namespace: ownership-graph
spec:
  type: ClusterIP
  selector:
    app: neo4j
  ports:
    - name: bolt
      port: 7687
      targetPort: bolt
    - name: http
      port: 7474
      targetPort: http
